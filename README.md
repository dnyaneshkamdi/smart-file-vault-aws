# smart-file-vault-aws
Serverless file management system built on AWS using S3, Lambda, DynamoDB, SNS, and CloudWatch. Automatically logs file metadata, validates file types, and sends real-time email alerts on every upload — zero server management.
